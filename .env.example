# The main Groq API key used for inference (e.g. Llama-3, etc)
GROQ_API_KEY=gsk_your_primary_api_key_here

# Fallback Groq API key used when the primary key hits a rate limit or expires
GROQ_API_KEY_FALLBACK=gsk_your_fallback_api_key_here
